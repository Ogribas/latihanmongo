<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <a href="/">Home</a>
    <% if (!products || products.length < 1) { %>
        <p>kontol</p>
    <% } else { %>
        <ul style="display: flex;flex-direction: column;gap: 2vh;">
            <li>Nama Produk: <%= products.name %></li>
            <li>Harga Produk: Rp.<%= products.harga %></li>
            <a href="/details/update/<%= products._id %>">Update</a>
            <form action="/details/delete/<%= products._id %>" method="post">
            <button type="submit">Delete</button>
            </form>

        </ul>
    <% } %>
         
   
</body>
</html>